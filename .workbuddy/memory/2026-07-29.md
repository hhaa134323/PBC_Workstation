# 2026-07-29

## v9 打包 + 审计员反馈复盘（01:13-01:25）

### 审计员反馈（v8）
1. 导入 PBC 清单报 "Unexpected token 'I', Internal S.." → 前端 .json() 解析 500 返回的 HTML
2. AI 配置测试连接一直失败 → 后端 model 为空时默认用 glm-5，百炼不认

### 修复（v9）
1. 导入 PBC 前端解析失败：importPbcList 改成先 r.text() 再 JSON.parse，失败返回 {ok:false,error:txt} 不抛异常
2. AI 测试连接默认 model：routes_config test_ai_connection 的 model 从 body.model → 配置文件 bl.get("model") → ai_models.model_classification → "qwen-plus"，不写死 glm-5

### commit
- d264bab fix: 导入PBC前端JSON解析失败 + AI测试连接默认model写死glm-5
- 已 push（Everything up-to-date）

### v9 打包
- 复制 v8 spec 改名 v9，73 秒完成
- PBC-Agent-v9.exe 15.9MB + _internal 共 120MB
- 本机 PowerShell Start-Process 被 WDAC 拦截（v8 能跑 v9 被拦，新文件扫描延迟）
- 审计员电脑没 WDAC，应该能跑

### 本次交付物
- v9 包发给审计员（含按钮修复 + 导入修复 + AI 测试修复）
- 作品说明材料 HTML（打包版 base64 内嵌 8 张截图 3.4MB）

### 教训
1. AI 测试连接的默认 model 不能写死——不同 AI 平台模型名不同，要从配置文件读
2. 前端 fetch 后端 500 时不能直接 .json()——后端异常返回 HTML，前端要兼容非 JSON 响应
3. 本机 WDAC 会拦新 exe——打包后本机测不了 v9，但审计员电脑能跑

## 待归档确认按钮无反应 修复（00:43）

### 用户反馈
待归档 tab 的"确认归档"按钮点了没反应

### 根因（2 个）
1. **briefingDelta.stock_total null 报错**（今日简报修复引入）
   - 行 1254: `briefingDelta.stock_total||0` 当 briefingDelta 是 null 时访问 .stock_total 报错
   - 修: `((briefingDelta&&briefingDelta.stock_total)||0)`
2. **fileDetail.item.confidence null 报错**
   - 行 1167: `:style` 里 `fileDetail.item.confidence` 没用可选链，item 是 null 时报错
   - 修: `fileDetail.item?.confidence`
3. **confirmReview id 类型不匹配**（核心）
   - pendingArchive.items[].id 是 number（1721）
   - 按钮 onclick 里的 id 是 string（"1711"）
   - `x.id===confirmId` 严格相等 number≠string → find 返回 undefined → if(!it) return
   - 修: `String(x.id)===String(confirmId)`

### 验收
- POST /confirm/1711 返回 200 OK，文件已归档
- pending-confirm 重新加载
- 无 PAGEERROR
- 按钮点击正常工作

### 改的文件
- app/static/index.html（3 处 null 保护 + confirmReview/openReclassifyConfirmReview 改 String 比较）

## 作品说明材料 HTML 完成（01:01）

### 内容结构
1. 标题+lead：让审计员把时间还给做判断出结论
2. 元信息：作品名/场景/功能/工具/数据来源/运行环境
3. 现状痛点：client portal 不愿用+手工整理遗漏重复+专业性受质疑
4. 现状对比表：4 行手工 vs 工作站
5. 核心功能 6 块：AI分类归档/待归档确认/风险信号卡/变更记录/五页签流水线/多项目
6. 使用前后：两段对比
7. 预期价值 3 卡：效率/风险/专业性
8. 推广可能：多项目+跨审计类型+本地单机无部署门槛
9. 页脚：coding agent + 百炼

### humanizer 去 AI 味改的
- "一眼看到底"→"风险集中在这一页"
- "这是效率跟专业判断的平衡点——AI干体力活人干判断活"→"AI干体力活人干判断活"（去套话）
- "老老实实每条都人工确认"→"每条都人工确认"
- "几分钟完事"→删
- "一目了然"→"直接显示"
- "完整来龙去脉"→"往下翻"
- "省出来的时间"→"省下的时间"
- "不用问人翻记忆"→删

### 截图 8 张（用户替换过的中文文件名）
01_coach_create_project/待初检/待归档/已完成/风险分析/变更记录/文件区/项目列表
视觉模型全确认无明显异常
