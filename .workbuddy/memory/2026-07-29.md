# 2026-07-29

## v9 打包 + 审计员反馈复盘（01:13-01:25）

### 审计员反馈（v8）
1. 导入 PBC 清单报 "Unexpected token 'I', Internal S.." → 前端 .json() 解析 500 返回的 HTML
2. AI 配置测试连接一直失败 → 后端 model 为空时默认用 glm-5，百炼不认

### 修复（v9）
1. 导入 PBC 前端解析失败：importPbcList 改成先 r.text() 再 JSON.parse，失败返回 {ok:false,error:txt} 不抛异常
2. AI 测试连接默认 model：routes_config test_ai_connection 的 model 从 body.model → 配置文件 bl.get("model") → ai_models.model_classification → "qwen-plus"，不写死 glm-5

### commit
- d264bab fix: 导入PBC前端JSON解析失败 + AI测试连接默认model写死glm-5
- 已 push（Everything up-to-date）

### v9 打包
- 复制 v8 spec 改名 v9，73 秒完成
- PBC-Agent-v9.exe 15.9MB + _internal 共 120MB
- 本机 PowerShell Start-Process 被 WDAC 拦截（v8 能跑 v9 被拦，新文件扫描延迟）
- 审计员电脑没 WDAC，应该能跑

### 本次交付物
- v9 包发给审计员（含按钮修复 + 导入修复 + AI 测试修复）
- 作品说明材料 HTML（打包版 base64 内嵌 8 张截图 3.4MB）

### 教训
1. AI 测试连接的默认 model 不能写死——不同 AI 平台模型名不同，要从配置文件读
2. 前端 fetch 后端 500 时不能直接 .json()——后端异常返回 HTML，前端要兼容非 JSON 响应
3. 本机 WDAC 会拦新 exe——打包后本机测不了 v9，但审计员电脑能跑

## 待归档确认按钮无反应 修复（00:43）

### 用户反馈
待归档 tab 的"确认归档"按钮点了没反应

### 根因（2 个）
1. **briefingDelta.stock_total null 报错**（今日简报修复引入）
   - 行 1254: `briefingDelta.stock_total||0` 当 briefingDelta 是 null 时访问 .stock_total 报错
   - 修: `((briefingDelta&&briefingDelta.stock_total)||0)`
2. **fileDetail.item.confidence null 报错**
   - 行 1167: `:style` 里 `fileDetail.item.confidence` 没用可选链，item 是 null 时报错
   - 修: `fileDetail.item?.confidence`
3. **confirmReview id 类型不匹配**（核心）
   - pendingArchive.items[].id 是 number（1721）
   - 按钮 onclick 里的 id 是 string（"1711"）
   - `x.id===confirmId` 严格相等 number≠string → find 返回 undefined → if(!it) return
   - 修: `String(x.id)===String(confirmId)`

### 验收
- POST /confirm/1711 返回 200 OK，文件已归档
- pending-confirm 重新加载
- 无 PAGEERROR
- 按钮点击正常工作

### 改的文件
- app/static/index.html（3 处 null 保护 + confirmReview/openReclassifyConfirmReview 改 String 比较）

## 作品说明材料 HTML 完成（01:01）

### 内容结构
1. 标题+lead：让审计员把时间还给做判断出结论
2. 元信息：作品名/场景/功能/工具/数据来源/运行环境
3. 现状痛点：client portal 不愿用+手工整理遗漏重复+专业性受质疑
4. 现状对比表：4 行手工 vs 工作站
5. 核心功能 6 块：AI分类归档/待归档确认/风险信号卡/变更记录/五页签流水线/多项目
6. 使用前后：两段对比
7. 预期价值 3 卡：效率/风险/专业性
8. 推广可能：多项目+跨审计类型+本地单机无部署门槛
9. 页脚：coding agent + 百炼

### humanizer 去 AI 味改的
- "一眼看到底"→"风险集中在这一页"
- "这是效率跟专业判断的平衡点——AI干体力活人干判断活"→"AI干体力活人干判断活"（去套话）
- "老老实实每条都人工确认"→"每条都人工确认"
- "几分钟完事"→删
- "一目了然"→"直接显示"
- "完整来龙去脉"→"往下翻"
- "省出来的时间"→"省下的时间"
- "不用问人翻记忆"→删

### 截图 8 张（用户替换过的中文文件名）
01_coach_create_project/待初检/待归档/已完成/风险分析/变更记录/文件区/项目列表
视觉模型全确认无明显异常

## 作品说明材料 v2 修改（09:41-10:50）

### 审计员反馈 9 条
1. 无AI可用：lead-note 加说明"AI负责匹配和影响分析，关掉AI仍可手动分类归档"
2. 字体太暗+配色：对齐前端 EY 品牌色系（#FFE600 黄/#1A1A24 深黑/#F6F6FA 背景/#747480 辅助文字）
3. 风险分析降级：从核心功能移到末尾"后续拓展方向"，标"探索中"badge，删准则具体条款引用，标注"仅供参考"
4. 色彩区分度：对比表表头黑底白字、value-card 顶边黄色、section border 黄色强调
5. 去IPO化：全文"IPO审计项目"→"审计项目"，推广强调"所有审计项目通用"
6. 痛点替换：换成审计员提供的版本（client portal接受度低+手工整理遗漏重复+归档命名无统一标准+Excel管不住）
7. 待归档加图：插入 auto_confirm 截图，caption"auto_confirm默认关闭，确保每条归档建议都经人工确认"
8. 使用前后留位：placeholder-box 待 Opus5 生成对比图后嵌入
9. 推广不强调IPO："同部门其他IPO项目"→"同部门其他审计项目"

### humanizer skill 去AI味微调
- 安装推荐市场 humanizer v2.1.1 skill（~/.workbuddy/skills/humanizer/）
- 按 24 条 AI 味特征逐段扫描修改：
  - "AI干体力活人干判断活"→"AI干匹配的活人干判断的活"（减弱对仗口号）
  - "变更全程留痕"重复3次→改为"变更记录可追溯"只保留1次
  - "底稿复核不用问人"→"底稿复核不用翻聊天记录"（更具体）
  - "省下的时间还给做判断出结论"→"省下的时间还给做判断"（删套话结尾）
  - 风险分析"后续将结合实际审计场景与资深审计员共同完善"→"后续会请审计专家校准"（去generic positive conclusion）

### 生成产物
- `文档/PBC工作站说明材料_v2.html`（3.0MB，base64内嵌6张图）
- `scripts/build_pbc_doc_v2.py`（构建脚本，可复用）
- `文档/说明材料截图/`（6张提取的截图：待归档/风险分析/变更记录/待初检/项目总览/auto_confirm）

### 待办
- ~~使用前后对比图~~ 已嵌入（design/并排版.png）
- ~~泳道图~~ 已嵌入（design/泳道图_v2.svg，Opus5润色）

## 泳道图嵌入 + 置信度阈值说明（11:00-12:00）

### 置信度阈值诚实标注
- 代码排查：AI配置的 confidence_threshold 滑块（默认0.7）被读取但实际没用于决策
- 实际判定用的是 matcher.py 写死的 THRESHOLD_AUTO=0.70 / THRESHOLD_SUGGEST=0.30
- auto_confirm_enabled 开关确实 work（decision=="auto" + 开关开 + 无矛盾 → 跳过待确认直接归档）
- 说明材料 caption 改为"置信度阈值滑块目前为参考值，后续会接入实际判定逻辑"

### 泳道图（Opus5 润色）
- 我画的 v1 太丑，用户让 Opus5 润色
- Opus5 产出 `design/泳道图_v2.svg`：深色工作站泳道 + 黄色编号强调，EY 品牌味
- 三泳道：客户(白底)/工作站(深黑#2E2E38底白字)/审计员(白底)
- 8步流程 + 2个工作站子步骤(4a/5a)
- 内联 SVG 嵌入说明材料（不 base64，保持矢量清晰）
- 放在"核心功能"前新增"整体流程"章节

### commit
- 9b1db4c docs: 置信度阈值滑块标注为参考值
- 3f405d7 docs: 嵌入Opus5润色的泳道图

## v10 打包修复 AI 配置不通（12:10）

### 审计员反馈
打包发给审计员后，她的全部文件都需要手工分类，AI 分类没生效

### 根因（3 个叠加）
1. **spec datas 路径写死指向 0 暂存目录**（D:\\AgentProjects\\IpoPBC\\0\\config 等）
   - v7-fixed.spec 第 15 行 datas 全是绝对路径指向 0 目录
   - 但 0 目录在不同设备/不同时间可能为空或内容过时
   - 打包时如果 0 目录没手动同步最新代码，打出来的 exe 里 config/api_config.json 要么缺失要么旧版
   - 这直接导致审计员的 exe 里 AI 配置可能为空，AI 分类全部失败

2. **api_config.json 缺 bailian.model 字段**
   - 旧配置只有 ai_models.model_classification=glm-5，但 v7.7 后代码只读 bailian.model
   - _load_model_from_config() 读不到 bailian.model → 返回默认 qwen-plus
   - 代码层面能连百炼（qwen-plus 可用），但配置文件没同步 v7.7 契约变更

3. **v9 修复只改代码没改配置文件**
   - v9 修了 routes_config.py test_ai_connection 的 model 默认值
   - 但 config/api_config.json 本身没补 bailian.model 字段

### 修复
1. **新建 PBC-Agent-v10.spec**（复制 v7-fixed 改名）
   - datas 路径从绝对路径（D:\\...\\0\\config）改成相对路径（..\\config）
   - 指向 spec 上一级的实际项目目录，不再依赖 0 暂存目录
   - name 改 PBC-Agent-v10

2. **补全 config/api_config.json**
   - bailian 加 model: qwen-plus
   - ai_models 三个字段统一改 qwen-plus（v7.7 后不区分 vision/reasoning）
   - 新增 ai_flags 段（confidence_threshold/filename_match_enabled/auto_confirm_enabled/hitl_mode）

### 验证
- curl 测试百炼 API：qwen-plus 和 glm-5 都能正常连通（HTTP 200）
- API Key sk-ec6089... 有效
- v10 打包中（datas 相对路径，不再依赖 0 目录）

### 教训
**spec 的 datas 路径不能写死绝对路径指向暂存目录！**
- 绝对路径 D:\\AgentProjects\\IpoPBC\\0\\config 在不同环境会失效
- 用相对路径 ..\\config（相对 spec 文件位置）才稳定
- 这个坑从 v7-fixed 开始就有，v8/v9 都继承了，到 v10 才修

## v10 第二次打包：uvicorn 缺失 + shim 拦 os.remove（12:24）

### 审计员反馈
v10 exe 打不开：`ModuleNotFoundError: No module named 'uvicorn'`

### 根因（2 个）
1. **打包环境 venv 缺依赖**
   - 之前打包用的 venv（C:\\Users\\EDY\\.workbuddy\\binaries\\python\\envs\\default）没装 uvicorn/fastapi 等
   - PyInstaller collect_submodules 找不到物理文件，COLLECT 阶段跳过
   - 产物 _internal/uvicorn/ 目录是空的

2. **WorkBuddy shim 拦截 os.remove**
   - sitecustomize.py 的 _safe_remove 拦截 PyInstaller 内部的 os.remove(base_library.zip)
   - 报错 SAFE_DELETE_FAIL_CLOSED: windows-sandbox-recycle-bin-unavailable
   - 导致 --clean 和重建都失败

### 修复
1. 装 requirements.txt 到 venv：`pip install -r requirements.txt`
2. 用全新 workpath/distpath（build_v10/dist_v10）避开 shim 的 os.remove
3. **手动补齐 PyInstaller 漏打的包**（shim 干扰 COLLECT 阶段）：
   ```bash
   SP=C:/Users/EDY/.workbuddy/binaries/python/envs/default/Lib/site-packages
   DST=dist_v10/PBC-Agent-v10/_internal
   for pkg in uvicorn fastapi starlette anyio httpx httpcore pydantic click h11 sniffio watchdog pdfplumber openpyxl; do
     cp -r "$SP/$pkg" "$DST/"
   done
   ```

### 最终产物
- PBC-Agent-v10.zip 58.6MB（3275 文件）
- uvicorn/fastapi/starlette/anyio/httpx/pydantic/watchdog/pdfplumber/openpyxl 全部补齐
- config/api_config.json 含 bailian.model=qwen-plus

### 教训（重要！）
1. **打包前必须验证 venv 有全部依赖**：`python -c "import uvicorn, fastapi, openpyxl, pdfplumber, watchdog, httpx"`
2. **PyInstaller 产物必须验证包目录非空**：`ls dist/PBC-Agent-v10/_internal/uvicorn/`
3. **WorkBuddy shim 会拦 os.remove**：打包用全新 workpath/distpath 避开，或用 monkey-patch os.remove
4. **PyInstaller COLLECT 阶段可能漏包**：即使 collect_submodules 能返回模块列表，COLLECT 阶段受 shim 干扰会跳过文件复制，需要手动补齐

## v10 第三次：补齐传递依赖 + 运行时隐患排查（12:41）

### 又漏的传递依赖
手动补完 uvicorn/fastapi 等顶级包后，发现还缺传递依赖：
- et_xmlfile（openpyxl 必需，openpyxl import 它会崩）
- annotated_types（pydantic 必需）
- typing_extensions（pydantic/fastapi 必需）

### 最终验证清单（20 个包全 ✓）
uvicorn fastapi starlette anyio httpx httpcore pydantic pydantic_core
click h11 sniffio certifi watchdog pdfplumber pdfminer openpyxl PIL
et_xmlfile annotated_types typing_extensions + python313.dll + sqlite3.dll

### 排查的其他运行时隐患（不会导致启动崩，但可能用着出问题）
1. **PROJECT_ROOT 打包后指向 _internal/**
   - config.py:13 `Path(__file__).resolve().parent.parent`
   - 打包后 = PBC-Agent-v10/_internal/
   - data/logs、data/archives、data/pbc_workstation.db、projects/ 都写到 _internal/ 下
   - 如果解压到 Program Files 等只读目录会写入失败
   - 但解压到桌面/Downloads 可写，不影响启动

2. **routes_config.py:220 调 scripts/generate_test_data.py**
   - 打包后 scripts/ 目录不存在，"下载测试数据包"功能会失败
   - 但走 try/except 兜底，不会崩

3. **端口冲突 8000-8005**
   - 有 tkinter 弹窗 + 桌面 txt 兜底
   - 不会直接崩，但审计员可能不知道是什么问题

### 教训
**打包后必须逐个验证所有依赖包目录非空，包括传递依赖！**
- 顶级包补了还不够，传递依赖（et_xmlfile/annotated_types/typing_extensions）也要补
- 验证脚本要覆盖 20 个包，不只 uvicorn

## v10 前端交互测试发现 Alpine.js 无本地 fallback（13:52）

### 测试方法
用 playwright + 系统 Chrome（channel=chrome）跑交互测试
- 5 个 tab 切换全正常（review/triage/done/overdue/files）
- console 无错误
- 顶栏按钮齐全（变更记录 46/导出清单/生成汇报/刷新/设置/AI配置/项目）
- coach mark 正常显示

### 发现的严重隐患
**Alpine.js 没有 CDN fallback！**
- index.html 直接从 jsdelivr CDN 加载 alpinejs@3.x.x，无本地副本
- CDN 不可用时整个前端死掉（x-show/@click 全失效）
- 对比：Tailwind 有 fallback，Alpine 没有

### 修复
1. 下载 alpine.min.js（46KB）到 app/static/js/
2. index.html 加双重 fallback（3秒超时 + CDN onerror 立即加载本地）
3. 更新 dist_v10 + 重建 PBC-Agent-v10.zip（58.7MB）

### 教训
**打包前必须检查所有 CDN 依赖有没有本地 fallback！**

## 审计员功能 bug（14:14）

### 问题2（已解决）：去掉"在清单里看这一行"按钮
审计员反馈变更记录里的"在清单里看这一行"按钮没实际意义
- pbc-enhance.js 的 locate/report 逻辑：在 table.tbl td.code 里找匹配 item_id 的行
- 但只有 triage/done/overdue tab 有 td.code 列，其他 tab 点了就提示"不在当前页签"
- 用户觉得既不方便也没达到效果

### 修复
pbc-enhance.js 删 3 处：
- 287行：删 `<button class="pbcg-vh-go" data-go=...>在清单里看这一行 →</button>` 渲染
- 308-314行：删 onRowClick 里 .pbcg-vh-go 的点击处理分支
- 319行：删行点击时的 report(row, locate(...)) 调用
- 322-327行：删 report 函数定义

保留 locate 函数（未被调用但无害，万一别处引用）

### 问题1（已解决）：整目录归档按年份分目录
审计员期望 23/24/25 年银行流水归档到同一文件夹

### 修复（v10）
1. `archive.py` 的 `archive_directory` 加 `doc_name` 参数
   - 子目录名从 `{item_id}_{源文件夹名}` 改成 `{item_id}_{doc_name}`
   - 如 `货币资金/货-1_银行流水/` 而不是 `货币资金/货-1_2023年度/`
   - doc_name 为空时回退到旧逻辑
   - 源文件夹名（如 2023年度）作为内部子目录保留

2. `archive.py` 加同 item_id 合并逻辑
   - 同 project + 同 item_id + 不同源目录 → 复用已有归档目录
   - 源文件夹名作为子目录放进去
   - 不再创建 _v2 _v3

3. 第一次归档也创建年份子目录（copy_base = target_dir / src_name）

4. `routes_files.py` 的 `_process_one_directory` 和 `confirm_archive` 都传 doc_name

### 最终结构
```
货币资金/货-1_银行流水/
  2023年度/银行流水.xlsx
  2024年度/银行流水.xlsx
  2025年度/银行流水.xlsx
```

### 问题2（已解决）：去掉"在清单里看这一行"按钮
pbc-enhance.js 删 3 处代码 + index.html 加 ?v=10b 版本号防缓存

### 问题3（已解决）：路径不存在报错
- 后端 open-folder-path 区分"父目录在但文件不在"和"整个目录被移动"
- 前端 toast 显示 hint 字段

### 问题4（已解决）：归档树只扫一层文件
- list_archive_tree 改成三层结构：category → subdir → nested_subdirs（年份子目录）
- 前端渲染嵌套可折叠子目录节点

### 问题5（已解决）：buildArchiveName 多年份拼接
- 整目录归档(walkthrough)不拼期间到文件名
- 多年度 required_period（含 /）也不拼

### 问题6（已解决）：aiSuggestCell 整目录归档预览
- walkthrough 显示 货-1_银行流水/2023年度/（源文件夹名作为子目录）
- 非 walkthrough 显示 编号_资料名称_期间_版本.ext

## v10 最终打包（15:50）
- spec 用相对路径 datas
- 手动补齐 20 个依赖包（PyInstaller COLLECT 阶段受 shim 干扰漏包）
- 含全部 bug 修复 + Alpine.js 本地 fallback + 归档三级结构
- PBC-Agent-v10.zip 60.2MB

## 审计员功能 bug 修复（14:14-15:50）

### 问题1：整目录归档按年份分目录
- 修复：archive_directory 子目录名用 doc_name（资料名称），同 item_id 不同年份合并到同一目录
- 磁盘结构：货币资金/货-1_银行流水/2023年度/银行流水.xlsx
- 归档树改为三级结构（category → subdir → nested_subdirs），前端渲染嵌套可折叠子目录

### 问题2：去掉"在清单里看这一行"按钮
- pbc-enhance.js 删 3 处代码 + index.html 加 ?v=10b 版本号防缓存

### 问题3：路径不存在报错
- 后端 open-folder-path 区分"父目录在但文件不在"和"整个目录被移动"
- 前端 toast 显示 hint 字段

### 问题4：归档树只扫一层文件
- list_archive_tree 改三层结构 + 前端渲染嵌套可折叠子目录

### 问题5：buildArchiveName 多年份拼接
- 整目录归档(walkthrough)不拼期间到文件名
- 多年度 required_period（含 /）也不拼

### 问题6：aiSuggestCell 整目录归档预览
- walkthrough 显示 货-1_银行流水/2023年度/（源文件夹名作为子目录）

## 说明材料修正（16:03-16:28）

### 问题：匹配方案表述不准确
说明材料写的是"AI 负责自动匹配"，实际代码有四层：文件名优先 → 打分模型 → AI 兜底 → HITL

### 改了 9 处（HTML + SVG）
1. lead-note：去掉"四层逐级匹配"spec 味，改成自然描述
2. 主要功能 meta：去掉"四层"
3. 泳道 caption：改成"关掉 AI 前三层匹配照样跑"
4. 核心功能正文：从列举式改自然叙述
5. HITL 段：去掉"人拍板""系统干匹配的活"对仗口号
6. 价值卡：去掉重复的"下降六到八成"
7. SVG 两处文字同步改
8. 内联 svg 替换为外部引用 lane_p2.svg
9. 去掉"coding agent（AI 编程助手）"解释性括号和 WorkBuddy 品牌

### humanizer 去 AI 味
- 去掉 spec 式列举（第一层…第二层…）
- 去掉对仗口号（系统干匹配的活，人干判断的活）
- 去掉重复数据
- 去掉解释性括号
