# 优化清单（基于决策树 v2 + Code Review）

## P0 — 必修（影响功能正确性 / 安全 / 体验）

| # | 场景 | 问题 | 文件 | 修复方案 |
|---|---|---|---|---|
| 1 | 2 扫描 | classify 失败时 item_id=None，后续 archive/period_check/write_pbc_list/update_status 全跳过 | routes_files.py _process_one_file_sync | classify 失败时也要把文件归档到 `archives/未分类/` + 写入 SQLite 标记 `unclassified` + 给用户弹 toast "AI 无法识别，请人工指定" |
| 2 | 2 扫描 | file_stable_size 不稳定跳过时无用户提示 | routes_files.py | 跳过的文件也要记录到 task results，返回 `{ok: false, skipped: true, reason: "file_unstable"}` 让前端弹 toast |
| 3 | 2 扫描 | write_pbc_list 写入可能丢失 Excel 条件格式 | excel_io.py write_pbc_list | 已用 load_workbook 增量改，但要再次验证条件格式保留。加单测：写一次后打开文件检查 conditional_formatting 规则数 |
| 4 | 2 扫描 | update_item_status 状态机校验失败时数据不一致 | excel_io.py + routes_files.py | 状态机失败要 rollback（已经写的 Excel 改回去）+ 返回 400 + 不写 SQLite state_changes |
| 5 | 3 复核 | 撤销归档（已提供→审核中）后端不允许，前端有此选项 | excel_io.py 状态机 + routes_pbc.py | 思路 6.1 加一条合法流转：已提供 → 已提供，审核中（撤销归档，Manager 权限）|
| 6 | 4 风险 | AI resolve 失败时如果连降级都失败，后端抛 500 | routes_risk.py resolve | 包 try/except，降级失败也返回 `{ok: false, error: "...", fallback_used: true}` 不抛 500 |
| 7 | 6 异常 | API Key 运行时 401 任务中断，无运行时校验机制 | ai_client.py + main.py | 启动时调一次真实 API 校验；运行时 401 时自动停止 watchdog + 弹 toast "API Key 失效，请检查 config" |
| 8 | 6 异常 | CDN 不可用时界面无样式不交互 | index.html | 加 CDN fallback：`<script>window.tailwind \|\| document.write('<link rel="stylesheet" href="/static/css/tailwind-local.css">')</script>` + 本地存一份 tailwind 最小 CSS |
| 9 | 6 异常 | SQLite 锁竞争（无 WAL）写入失败 | db.py | `PRAGMA journal_mode=WAL;` + 写入失败 retry 3 次（50ms 间隔）|
| 10 | 1 首启 | 端口全占时应用无法启动，无友好提示 | main.py | 端口全占时弹一个 tkinter/messagebox 提示框（不只是控制台日志），让用户知道要关占用进程 |

## P1 — 重要（体验优化）

| # | 场景 | 问题 | 文件 | 修复方案 |
|---|---|---|---|---|
| 11 | 1 首启 | 无项目时只提示创建，没有"加载示例数据"快捷入口 | index.html 首启引导 | 首启 Step 2 加"加载示例数据"按钮，一键复制 demo 项目到新项目 |
| 12 | 1 首启 | demo 项目就绪失败时首次加载较慢 | main.py lifespan | 启动时异步创建 demo（不阻塞 lifespan）+ 前端显示"正在初始化示例项目..." |
| 13 | 2 扫描 | 客户文件夹未配置时卡在配置界面，无强制退出 | index.html 设置面板 | 设置面板加"取消"按钮，允许用户先看 demo 再配置 |
| 14 | 2 扫描 | 文件夹不存在时只提示"文件夹不存在" | routes_files.py config | 提示更友好：列出"请检查路径：{path}，建议改指向 OneDrive 挂载点" |
| 15 | 2 扫描 | 文件夹为空时白点一次按钮 | routes_files.py scan-folder | 扫描 0 文件时返回 `{files_found: 0, suggestion: "请先在 OneDrive 里放文件"}`，前端 toast 友好提示 |
| 16 | 2 扫描 | PDF 完全不可读时解析失败无降级 | file_parser.py | PDF 完全不可读时也要返回 `{text: "", error: "pdf_unreadable"}`，让 classify 继续跑（结果就是低置信度），不要中断流程 |
| 17 | 3 复核 | Tab 2 空状态展示不友好 | index.html | 空状态加插画/引导文案"暂无待复核项，请先扫描文件 →" |
| 18 | 4 风险 | Tab 3 无超期项时显示"无超期"过于简陋 | index.html | 加庆祝动画"🎉 项目完成度 100%"或"健康度优秀"标识 |
| 19 | 4 风险 | 热力图无数据时空白 | index.html | 空状态显示"暂无风险数据，待有超期项后自动填充" |
| 20 | 5 项目 | 项目抽屉 UI 响应失败无法展开（极端情况） | index.html | 加错误边界：Alpine.js x-cloak + try/catch + 重试按钮 |
| 21 | 6 异常 | 半截文件被跳过时无用户提示 | routes_files.py | 跳过的文件返回 `{skipped: true, reason: "file_unstable"}`，前端 toast "⚠ 文件 X 正在被写入，已跳过，稍后请重新扫描" |
| 22 | 7 边界 | 项目被删除后 localStorage 还存着上次项目，fallback 无提示 | index.html | fallback 时弹 toast "上次访问的项目已删除，已切换到 {项目名}" |
| 23 | 7 边界 | 空清单时扫描文件无法匹配 | routes_files.py | 空清单时扫描到的文件全部归档到 `archives/未分类/` + toast "PBC 清单为空，请先导入清单或手动创建条目" |

## P2 — 加分（可选）

| # | 场景 | 问题 | 修复方案 |
|---|---|---|---|
| 24 | 2 扫描 | hard delete 项目不可恢复 | 删除前弹确认对话框 + 5 秒倒计时"即将永久删除" |
| 25 | 6 异常 | retry 第 2/3 次成功时延迟高，无进度提示 | 前端轮询 task status 时显示"AI 第 N 次重试中..." |
| 26 | 6 异常 | 找不到 URL 无法访问（极端无浏览器场景） | 生成桌面快捷方式 + 启动日志写到桌面 txt |

---

# 修复优先级

- **第一批（P0 全部，10 项）**：必须修，影响功能/安全/体验
- **第二批（P1 前 5 项）**：影响审计员测试体验
- **第三批（P1 后 8 项）**：体验加分
- **P2 3 项**：可选

# 修复后验证

每修一项都要：
1. 改动对应的代码
2. 启动服务测试该场景
3. 在决策树 HTML 里把该节点从 ❌→✅ 或 ⚠️→✅
4. 更新 docs/优化清单.md 状态
